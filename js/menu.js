/* =========================================================
   ФАЙЛ: js/menu.js
   ЗА ЧТО ОТВЕЧАЕТ:
   - открывает и закрывает выпадающее мобильное меню по нажатию на burger;
   - закрывает меню после клика по любому пункту;
   - используется только на планшетах и телефонах (на десктопе burger скрыт через CSS).
   ========================================================= */

/* =========================================================
   БЛОК 5. МОБИЛЬНОЕ МЕНЮ

   За что отвечает:
   - открывает и закрывает выпадающее меню на телефоне;
   - закрывает меню после выбора пункта.

   Почему так:
   - класс .open используется в CSS;
   - без .open меню скрыто;
   - с .open меню становится видимым.
   ========================================================= */
addEvent(burger, "click", function () {
  toggleClass(mainNav, "open");
});

if (mainNav) {
  var menuLinks = mainNav.getElementsByTagName("a");

  for (var i = 0; i < menuLinks.length; i++) {
    addEvent(menuLinks[i], "click", function () {
      removeClass(mainNav, "open");
    });
  }
}


