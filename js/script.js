/* =========================================================
   ФАЙЛ: js/script.js
   ЗА ЧТО ОТВЕЧАЕТ:
   - главный файл-оркестратор; запускает приложение после загрузки DOM:
     • восстанавливает выбранный язык из localStorage;
     • запускает первичную отрисовку календаря.
   - сюда же удобно добавлять любую новую инициализацию.

   ПОДКЛЮЧАЕТСЯ ПОСЛЕДНИМ — все вспомогательные модули должны быть
   уже загружены к моменту его исполнения.
   ========================================================= */

/* =========================================================
   БЛОК 15. ЗАПУСК СКРИПТА

   За что отвечает:
   - применяет сохраненный язык;
   - рисует календарь после загрузки.

   Почему так:
   - сайт сразу открывается на последнем выбранном языке;
   - календарь появляется без дополнительных действий.
   ========================================================= */
setLanguage(loadLanguage());
renderCalendar();
