/* =========================================================
   ФАЙЛ: js/elements.js
   ЗА ЧТО ОТВЕЧАЕТ:
   - один раз находит и сохраняет все ключевые DOM-элементы:
     header, heroSection, burger, mainNav, langButtons, calendar и т.д.
   - переменные становятся глобальными, и остальные модули их переиспользуют.

   ПОДКЛЮЧАЕТСЯ ТРЕТЬИМ — после utils.js, до header.js / menu.js / language.js / calendar.js.
   ========================================================= */

/* =========================================================
   БЛОК 3. ПОИСК ОСНОВНЫХ ЭЛЕМЕНТОВ

   За что отвечает:
   - находит шапку, кнопку мобильного меню и само меню;
   - эти элементы используются дальше в скрипте.

   Почему так:
   - поиск выполняется один раз при загрузке скрипта;
   - это проще и быстрее, чем искать элементы заново в каждой функции.
   ========================================================= */
var header = document.getElementById("siteHeader");
var burger = document.getElementById("burger");
var mainNav = document.getElementById("mainNav");
var heroSection = document.getElementById("about");


